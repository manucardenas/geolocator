<h1><%= @place.name %></h1>
<%= link_to "All Places", places_path %>
<%= button_to 'Delete Location', @place, method: :delete %>

<hr />
<%= react_component 'Map',
    props: {
      coordinates: [@place.longitude, @place.latitude],
      centerOnUser: false
    } %>
